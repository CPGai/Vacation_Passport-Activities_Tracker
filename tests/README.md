# Pruebas

Ejecutar desde la carpeta del proyecto:

`node --test tests/passport-core.test.mjs`

El archivo contiene las pruebas RED/GREEN del núcleo puro. Las comprobaciones de impresión requieren revisar `index.html` en navegador con Letter vertical, 100% y dúplex por borde largo.
